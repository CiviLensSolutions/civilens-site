export function SectionRule() {
  return (
    <div
      style={{
        height: 1,
        width: "100%",
        background: "linear-gradient(to right, transparent 0%, transparent 12%, var(--fg-2) 100%)",
      }}
      aria-hidden="true"
    />
  );
}
